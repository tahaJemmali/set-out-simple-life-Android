<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tn.esprit.setoutsimplelife">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".tutorial.finance"></activity>
        <activity android:name=".tutorial.gain" />
        <activity android:name=".tutorial.commun_task" />
        <activity android:name=".tutorial.project_schema" />
        <activity android:name=".tutorial.simple_task" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".tutorial.welcome" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>